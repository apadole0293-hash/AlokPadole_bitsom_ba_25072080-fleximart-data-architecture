## OLTP vs OLAP Comparison

The source MySQL database is optimized for OLTP operations such as inserts and updates.
The data warehouse is optimized for OLAP workloads, enabling complex analytical queries
across large datasets with improved performance.

Star schema design simplifies queries and improves readability for business analytics.
